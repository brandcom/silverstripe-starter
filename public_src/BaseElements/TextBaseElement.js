// Einbindung der Styles für das TextBaseElement
import "./TextBaseElement.scss";

// Funktion zur Handhabung des TextBaseElements
export function TextBaseElement() {
    // Überprüfen, ob ein TextBaseElement auf der aktuellen Seite vorhanden ist
    if (!document.querySelector(".TextBaseElement")) {
        // Wenn nicht, wird die Funktion beendet
        return;
    }

    // Füge hier die gewünschte Funktionalität für das TextBaseElements hinzu
    // ...
}
